<routes xmlns="http://camel.apache.org/schema/spring">
    <route id="mainTestRoute">
        <from uri="direct:hello"/>
        <log message="Test HELLO CALL"/>
    </route>

    <route id="timerRoute">
        <from uri="timer://foo?fixedRate=true&amp;period=1000"/>
        <log message=" ++++++++++++++ Test TIMER  ++++++++++++++ "/>
    </route>
</routes>